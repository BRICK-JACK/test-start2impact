
const config = {
    database: {
      username: "giacomo",
      password: "bergamini",
      database: "test-start2impact",
      host: "localhost",
      dialect: "mysql"
    }
}

export default config